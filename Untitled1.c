#include <stdio.h>

int main () {

  int kase, cnt = 0;
  scanf ( "%d", &kase );
  while ( kase-- ) {
    long long a, b;
    char c;
    scanf ( "%lld %lld %c", &a, &b, &c );

    //Muhahaha
  }

  return 0;
}
