#include <bits/stdc++.h>

#define forn(i, n) for (int i = 0; i < int(n); i++)

using namespace std;

// vector
vector<int> nodes;
vector<vector<int>> edge;
vector<int> who;

int vector_ninja() {
    int n, m;
    cin >> n >> m;

    nodes.resize(n);
    edge.resize(m);
}

