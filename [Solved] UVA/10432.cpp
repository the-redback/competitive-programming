#include <stdio.h>
#include <math.h>

main()
{
    double m,a;
    int b;
    while(scanf("%lf %d",&a,&b)==2)
    {
        m=sin((2*(acos(-1)))/(float)b);
        m=(m*b*a*a)/(float)2;
        printf("%.3lf\n",m);
    }
    return 0;
}
